interface:
  display_name: "Skill Name"
  short_description: "Short human-facing summary"
  # Replace $skill-name with the exact skill name so agents know which skill to invoke.
  default_prompt: "Use $skill-name to complete the requested task."

policy:
  allow_implicit_invocation: true
