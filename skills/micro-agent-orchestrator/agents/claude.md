# Claude Notes

Invoke this skill proactively when implementation spans services, utilities, APIs, and tests. Use it to select the smallest writer skills instead of doing one broad undifferentiated pass.
