interface:
  display_name: "Repository Map"
  short_description: "Map repository context and per-file CSV navigation with optional AI enrichment"
  default_prompt: "Use $repository-map to generate compact project context, a per-file CSV map, and enrich pending rows for faster cold starts."

policy:
  allow_implicit_invocation: true
