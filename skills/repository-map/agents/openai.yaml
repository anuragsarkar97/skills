interface:
  display_name: "Repository Map"
  short_description: "Map repository context, files, and symbols"
  default_prompt: "Use $repository-map to generate compact project context and a semantic navigation map."

policy:
  allow_implicit_invocation: true
