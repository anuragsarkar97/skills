# Claude Notes

Invoke this skill proactively for `/remember`, "remind me", "notify me", "later today", "tonight", or "tomorrow" requests that should become local macOS notifications. Use the bundled script and confirm ambiguous reminder text or time before scheduling.
