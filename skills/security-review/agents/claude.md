# Claude Notes

Invoke this skill proactively for auth, permissions, tenant isolation, secrets, PII, uploads, webhooks, external callbacks, dependency trust, and security-sensitive production changes.
