interface:
  display_name: "Decision Memory"
  short_description: "Record and retrieve engineering decisions"
  default_prompt: "Use $decision-memory to record or retrieve the relevant engineering decision."

policy:
  allow_implicit_invocation: true
