interface:
  display_name: "API Review"
  short_description: "Review API contracts and behavior"
  default_prompt: "Use $api-review to review API contracts, behavior, and compatibility."

policy:
  allow_implicit_invocation: true
