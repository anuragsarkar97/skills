# Claude Notes

Invoke this skill proactively when a skill misses a trigger, produces weak guidance, overlaps another skill, needs routing updates, or needs validation/script support. Keep changes small, evidence-based, and validated with `npm test`.
