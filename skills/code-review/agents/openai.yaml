interface:
  display_name: "Code Review"
  short_description: "Review code for bugs and regressions"
  default_prompt: "Use $code-review to review this change and lead with findings."

policy:
  allow_implicit_invocation: true
